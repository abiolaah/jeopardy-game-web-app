// Define the Team type
type Team = {
  id: number;
  name: string;
  score: number;
};

type Question = {
    id: number;
    category: string;
    point: string;
    question: string;
    // options: { id: string; option: string }[];
    answer: string;
  };

   